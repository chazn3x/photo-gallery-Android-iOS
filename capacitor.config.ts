import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chaznex.photogallery',
  appName: 'Photo Gallery',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
